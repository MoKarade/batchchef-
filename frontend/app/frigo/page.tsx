import { InventoryPage } from "@/components/features/InventoryPage";

/**
 * /frigo = new friendlier name for inventory. Renders the existing
 * InventoryPage; logic unchanged in Phase 1.
 */
export default function Page() {
  return <InventoryPage />;
}
